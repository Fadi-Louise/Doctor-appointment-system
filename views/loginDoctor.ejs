<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Link to Bootstrap CSS from a CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans">

    <!-- Internal styles for the document -->
    <style>
        /* Body background color */
        body {
            background-color: #e4e4e4;
            background-image: url('/assets/bg.png');
            background-size: cover;
            font-family: 'American Typewriter', serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        /* Styling for the buttons with thicker border */
        .btn-outline-dark {
            border-width: 2px;
        }

        /* Styling for the card */
        .card {
            max-width: 500px;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Styling for the form */
        form {
            padding: 20px;
        }

        .center-btn {
            text-align: center;
            margin-top: 20px;
        }

        /* Styling for the responsive image */
        .responsive-img {
            max-width: 100%;
            height: auto;
            margin-top: 25px;
        }

        .footer {
            position: absolute;
            bottom: 0;
            width: 100%;
            text-align: center;
            padding: 10px;
            font-size: 0.8em;
        }

        .login-links a {
            color: black;
            text-decoration: none;
        }

        .login-links a:hover {
            text-decoration: underline;
        }

        .login-links {
            margin-top: 10px;
        }

        .login-links a:not(:last-child) {
            margin-bottom: 10px;
            display: block;
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="card-body">
            <h1 class="text-center mb-4">Doctor Login</h1>

            <form method="POST" action="/api/doctors/login">
                <div class="form-group mb-3">
                    <label for="email">Email</label>
                    <input type="email" class="form-control" id="email" name="email" required>
                </div>

                <div class="form-group mb-3">
                    <label for="password">Password</label>
                    <input type="password" class="form-control" id="password" name="password" required>
                </div>

                <button type="submit" class="btn btn-outline-dark w-100">Login</button>
            </form>
            <div class="login-links text-center">
                <a href="/api/doctors/signup">No Account? Sign up as a Doctor</a>
                <a href="/api/patients/signup">OR Sign up as a Patient</a>
            </div>
        </div>
    </div>

    <div class="footer">
        © 2023 Med. Created By Fadi. All rights reserved.
    </div>

    <!-- Bootstrap JS script from a CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>

</html>
